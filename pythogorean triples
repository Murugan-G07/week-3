#include<math.h>
#include <stdio.h>


int main() {
   int a,b;
   float c;
   scanf("%d\n%d\n%d",&a,&b,&c);
   c=a*a+b*b;
   c=sqrt(c);
   if(floor(c)==c){
       printf("yes");
   }
   else{
       printf("no");
   }
   return 0;
 
}
